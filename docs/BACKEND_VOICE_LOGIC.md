# 🎤 Логика наследования голосов

## Приоритет выбора (сверху вниз):

1. Голос, явно указанный для `GENERATION`
2. Голос, явно указанный для `REPLIC`
3. Голос, явно указанный для говорителя (`SPEAKERS`)
4. Стандартный голос для комбинации `{race}_{gender}_{NPCSoundID}` (фоллбек)
